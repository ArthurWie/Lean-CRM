{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Least-privilege capability set for Phase 1: SQL access only (no shell/fs scopes). Mitigates T-01-EOP. sql:default grants load/select/close; allow-execute is required for INSERT/UPDATE/DELETE (seeding + writes).",
  "windows": ["main"],
  "permissions": ["core:default", "sql:default", "sql:allow-execute"]
}
